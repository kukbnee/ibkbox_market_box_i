<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ibk.sb.restapi.biz.service.order.repo.OrderDeliveryRepo">

    <sql id="MARKET"><include refid="com.ibk.sb.restapi.biz.service.common.repo.SchemaRepo.MARKET"/></sql>

    <!--    PagingMapper ID-->
    <sql id="pagingHeader"><include refid="com.ibk.sb.restapi.biz.service.common.repo.PageRepo.pageHeader"/></sql>
    <sql id="pagingFooter"><include refid="com.ibk.sb.restapi.biz.service.common.repo.PageRepo.pageFooter"/></sql>

    <!-- 구매자 배송정보 가져오기 -->
    <select id="getDeliveryInfo" parameterType="RequestPayVO" resultType="OrderDeliveryVO">
        SELECT
            TBMPIM.RECV
             ,TBMPIM.ZPCD
             ,TBMPIM.ADR
             ,TBMPIM.DTL_ADR
             ,TBMPIM.CNPLONE
             ,TBMPIM.CNPLTWO
             ,TBMPIM.DLPL_USE_YN
             ,TBMPIM.MMBRSTTS_ID
        FROM
            <include refid="MARKET"/>TB_BOX_MKT_PUCS_INF_M TBMPIM
        WHERE 1=1
          AND TBMPIM.PUCS_USIS_ID = #{pucsUsisId}
    </select>

</mapper>